generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/workspace/wco1/data/model/UMLClassDiagram.ecore"

with commands {
	remove one PathElementCS where {pathName = one Attribute}
}
