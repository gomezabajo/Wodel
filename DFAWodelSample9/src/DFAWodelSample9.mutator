generate 2 mutants
in "out/"
from "model/"
metamodel "C:/eclipse/runtime-EclipseApplication/DFAWodelSample9/model/DFAAutomaton.ecore"

with commands {
	t = select one Transition
	modify one Transition where {self <> t and src = t.src} with {swapref(symbol, t.symbol)}
}