generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/workspace/ucl4/data/model/UMLClassDiagram.ecore"

with commands {
	create AssociationClass with {name = random-string(1, 4)}
}
