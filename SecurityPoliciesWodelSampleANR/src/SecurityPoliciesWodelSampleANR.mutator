generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/SecurityPoliciesWodelSampleANR/data/model/SecurityPolicy.ecore"

with commands {
	t = select one RuleType
	setp = select all Parameter where {type in t->parameters}
	p = select each Parameter in setp where {type = t->parameters}
	create Rule with {type = t, parameters = p}
}