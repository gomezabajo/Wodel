generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/SecurityPoliciesWodelSamplePPD/data/model/SecurityPolicy.ecore"

with commands {
	r = select one Rule where {parameters->children <> null}
	p = select one Parameter in r->parameters where {children <> null}
	c = select one Parameter in closure(p->children)
	remove p from r->parameters
	modify r with {parameters = c}
}