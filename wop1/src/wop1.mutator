generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/workspace/wop1/data/model/UMLClassDiagram.ecore"

with commands {
	oper = select one Operation where {params <> null}
	p = select one Parameter in oper->params
	modify oper with {params -= p, params += p}
}
