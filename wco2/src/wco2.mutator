generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/wco2/data/model/UMLClassDiagram.ecore"

with commands {
	path = select one PathElementCS where {pathName = one Attribute}
	att = select one Attribute in path->pathName
	modify att with {type = one Class}
}