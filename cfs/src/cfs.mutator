generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/workspace/cfs/data/model/DFAAutomaton.ecore"

with commands {
	s = create State with {isFinal = true}
    create Transition with {tar = s, symbol = one Symbol}
}