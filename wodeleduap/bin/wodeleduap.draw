metamodel "/wodeleduap/data/model/StackAutomaton.ecore"

Automaton: diagram {
	State(isInitial): markednode
	State(not isFinal): node shape=circle
	State(isFinal): node shape=doublecircle
	Transition(src, tar): edge label={symbol.symbol, stackSymbol->symbol.symbol, action.operation(action->symbol.symbol)}
}