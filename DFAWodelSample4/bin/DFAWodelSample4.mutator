generate 2 mutants
in "out/"
from "model/"
metamodel "C:/eclipse/runtime-EclipseApplication/DFAWodelSample4/model/DFAAutomaton.ecore"

with commands {
	remove one State where {isInitial = false}
	remove all Transition where {src = null}
	remove all Transition where {tar = null}
}