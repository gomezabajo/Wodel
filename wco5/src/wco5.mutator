generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/workspace/wco5/data/model/UMLClassDiagram.ecore"

with commands {
	inv = select one InvariantCS where {exp <> null}
	create LogicExpCS in inv->exp with {op = 'not'}
}
