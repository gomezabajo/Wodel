generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/was2a/data/model/UMLClassDiagram.ecore"

with commands {
	as = select one Association
    rel = create Aggregation with {src = as->src, tar = as->tar}
    modify rel with {copy(name, as.name)}
    remove as
}