generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/was2j/data/model/UMLClassDiagram.ecore"

with commands {
	dep = select one Dependency
	rel = create Aggregation with {src = dep->src, tar = dep->tar}
	modify rel with {copy(name, dep.name)}
	remove dep
}
