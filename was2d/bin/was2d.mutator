generate 2 mutants
in "data/out/"
from "data/model/"
metamodel "C:/eclipse/runtime-EclipseApplication/was2d/data/model/UMLClassDiagram.ecore"

with commands {
	comp = select one Composition
	rel = create Association with {src = comp->src, tar = comp->tar}
	modify rel with {copy(name, comp.name)}
	remove comp
}
