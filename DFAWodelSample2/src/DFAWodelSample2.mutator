generate 2 mutants
in "out/"
from "model/"
metamodel "C:/eclipse/runtime-EclipseApplication/DFAWodelSample2/model/DFAAutomaton.ecore"

with commands {
	s = create State with {name = random-string(1,4)}
	t = create Transition with {tar = s, symbol = one AlphabetSymbol}
}